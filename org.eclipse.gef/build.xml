<?xml version="1.0" encoding="UTF-8"?>
<project name="org.eclipse.gef" default="build.jars" basedir=".">

	<property name="bootclasspath" value=""/>
	<property name="ws" value="win32"/>
	<property name="os" value="win32"/>
	<property name="arch" value="x86"/>

	<target name="init" depends="properties">
		<property name="plugin" value="org.eclipse.gef"/>
		<property name="version.suffix" value="_2.0.1"/>
		<property name="full.name" value="${plugin}${version.suffix}"/>
		<property name="temp.folder" value="${basedir}/temp.folder"/>
		<property name="plugin.destination" value="${basedir}"/>
		<property name="build.result.folder" value="${basedir}"/>
	</target>

	<target name="properties" if="eclipse.running">
		<property name="build.compiler" value="org.eclipse.jdt.core.JDTCompilerAdapter"/>
	</target>

	<target name="build.update.jar" depends="init">
		<delete dir="${temp.folder}"/>
		<mkdir dir="${temp.folder}"/>
		<antcall target="build.jars"/>
		<antcall target="gather.bin.parts">
			<param name="destination.temp.folder" value="${temp.folder}/"/>
		</antcall>
		<zip zipfile="${plugin.destination}/${full.name}.jar" basedir="${temp.folder}/${full.name}" filesonly="false"/>
		<delete dir="${temp.folder}"/>
	</target>

	<target name="gather.bin.parts" depends="init" if="destination.temp.folder">
		<mkdir dir="${destination.temp.folder}/${full.name}"/>
		<mkdir dir="${destination.temp.folder}/${full.name}/runtime"/>
		<copy file="${build.result.folder}/runtime/gef.jar" todir="${destination.temp.folder}/${full.name}/runtime"/>
		<copy todir="${destination.temp.folder}/${full.name}">
			<fileset dir="${basedir}" includes="plugin.properties,plugin.xml,icons/*.gif"/>
		</copy>
	</target>

	<target name="runtime/gef.jar" depends="init" unless="runtime/gef.jar">
		<property name="destdir" value="${temp.folder}/runtime/gef.jar.bin"/>
		<delete dir="${temp.folder}/runtime/gef.jar.bin"/>
		<mkdir dir="${temp.folder}/runtime/gef.jar.bin"/>
		<!-- compile the source code -->
		<javac destdir="${temp.folder}/runtime/gef.jar.bin" failonerror="false" verbose="true" debug="on" includeAntRuntime="no" bootclasspath="${bootclasspath}" classpath="../org.eclipse.swt.win32/ws/${ws}/swt.jar;../org.eclipse.swt/bin;../org.eclipse.draw2d/runtime/draw2d.jar;../org.eclipse.core.runtime/runtime.jar;../org.eclipse.core.resources/resources.jar;../org.eclipse.ui/workbench.jar;../org.eclipse.core.resources.win32/resources.jar;../org.eclipse.core.runtime/bin;../org.eclipse.ui.win32/workbenchwin32.jar;../org.eclipse.core.resources/bin;../org.eclipse.ui.win32/uipatch.jar;../org.eclipse.core.boot/bin;../org.eclipse.ui/bin;../org.eclipse.swt/ws/${ws}/swt.jar;../org.eclipse.ui.win32/workbench.jar;../org.eclipse.swt/ws/${ws}/swtpatch.jar;../org.eclipse.core.boot/boot.jar;../org.eclipse.swt.win32/ws/${ws}/swtpatch.jar;../org.eclipse.ui/uipatch.jar;../org.eclipse.draw2d/bin">
			<src path="src/"/>
		</javac>
		<!-- copy necessary resources -->
		<copy todir="${temp.folder}/runtime/gef.jar.bin">
			<fileset dir="src/" excludes="**/*.java"/>
		</copy>
		<mkdir dir="${build.result.folder}/runtime"/>
		<jar jarfile="${build.result.folder}/runtime/gef.jar" basedir="${temp.folder}/runtime/gef.jar.bin"/>
		<delete dir="${temp.folder}/runtime/gef.jar.bin"/>
	</target>

	<target name="runtime/gefsrc.zip" depends="init" unless="runtime/gefsrc.zip">
		<mkdir dir="${build.result.folder}/runtime"/>
		<zip zipfile="${build.result.folder}/runtime/gefsrc.zip" filesonly="false">
			<fileset dir="src/" includes="**/*.java"/>
		</zip>
	</target>

	<target name="build.jars" depends="init">
		<available property="runtime/gef.jar" file="${build.result.folder}/runtime/gef.jar"/>
		<antcall target="runtime/gef.jar"/>
	</target>

	<target name="build.sources" depends="init">
		<available property="runtime/gefsrc.zip" file="${build.result.folder}/runtime/gefsrc.zip"/>
		<antcall target="runtime/gefsrc.zip"/>
	</target>

	<target name="build.zips" depends="init">
	</target>

	<target name="gather.sources" depends="init" if="destination.temp.folder">
		<mkdir dir="${destination.temp.folder}/${full.name}/runtime"/>
		<copy file="${build.result.folder}/runtime/gefsrc.zip" todir="${destination.temp.folder}/${full.name}/runtime"/>
	</target>

	<target name="gather.logs" depends="init" if="destination.temp.folder">
		<mkdir dir="${destination.temp.folder}/${full.name}/runtime"/>
		<copy file="${temp.folder}/runtime/gef.jar.bin.log" todir="${destination.temp.folder}/${full.name}/runtime"/>
	</target>

	<target name="clean" depends="init">
		<delete file="${build.result.folder}/runtime/gef.jar"/>
		<delete file="${build.result.folder}/runtime/gefsrc.zip"/>
		<delete file="${plugin.destination}/${full.name}.jar"/>
		<delete file="${plugin.destination}/${full.name}.zip"/>
		<delete dir="${temp.folder}"/>
	</target>

	<target name="refresh" depends="init" if="eclipse.running">
		<eclipse.refreshLocal resource="${plugin}" depth="infinite"/>
	</target>

	<target name="zip.plugin" depends="init">
		<delete dir="${temp.folder}"/>
		<mkdir dir="${temp.folder}"/>
		<antcall target="build.jars"/>
		<antcall target="build.sources"/>
		<antcall target="gather.bin.parts">
			<param name="destination.temp.folder" value="${temp.folder}/"/>
		</antcall>
		<antcall target="gather.sources">
			<param name="destination.temp.folder" value="${temp.folder}/"/>
		</antcall>
		<delete>
			<fileset dir="${temp.folder}" includes="**/*.bin.log"/>
		</delete>
		<zip zipfile="${plugin.destination}/${full.name}.zip" basedir="${temp.folder}" filesonly="true"/>
		<delete dir="${temp.folder}"/>
	</target>

</project>
